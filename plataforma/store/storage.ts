// DEPRECATED — a persistência migrou para o Supabase (ver
// ../supabase/client.ts e ../store/PlatformContext.tsx). Este arquivo foi
// mantido apenas como ponto de referência histórico e não é mais importado.
//
// O estado da plataforma (auth, conteúdo, progresso) agora vive no Postgres +
// Storage do Supabase; nada é gravado em localStorage.

export {};
